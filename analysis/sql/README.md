# sql

Store SQL queries used to extract or define source data.

Keep reusable queries here instead of embedding long SQL blocks directly in notebooks. Use clear filenames that describe the source table, cohort, or analytical purpose.
